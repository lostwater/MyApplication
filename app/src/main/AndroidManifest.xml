<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.turtlejk.myapplication">
    //摄像头权限
    <uses-permission android:name="android.permission.CAMERA"/>
    //SD卡读写权限
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        //android:theme="@android:style/Theme.NoTitleBar.Fullscreen">


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragment.FragmentsActivity"/>
        <activity android:name=".Imagepicker.WxDemoActivity"/>
        <activity android:name=".Imagepicker.ImagePickerActivity"/>
        <activity android:name=".Imagepicker.ImagePreviewandDelActivity"/>
        <activity android:name=".util.FileUploadActivity">

        </activity>
    </application>

</manifest>